# Issue chooser config. Disables blank issues so reporters pick a template,
# and routes security reports to private disclosure (never a public issue).
blank_issues_enabled: false
contact_links:
  - name: Security vulnerability
    url: https://github.com/<GITHUB_OWNER>/<REPO>/security/advisories/new
    about: Report security issues privately — do not open a public issue.
