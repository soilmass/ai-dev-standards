# Flow-back Log

The library's memory of what real projects taught it. Every finding a consuming project records in its debt-log "Library flow-back" section (see `08-maintenance/debt-log.template.md`) lands here with an explicit **disposition** — no finding is silently dropped. This is the durable, cross-project ledger that `00-governance/standards-lifecycle.md` §6 (the feedback loop) feeds; `scripts/check-flowback.sh` validates its integrity on every push/PR via suite CI.

**How an entry flows:** project debt-log finding → reviewed during a currency pass or ad-hoc → row added here with a disposition → if `patched`, the change ships under a tag named in the row (doc↔config same-commit per root `CLAUDE.md`); if `deferred`, the row carries a trigger for when to revisit; if `no-change`, a one-line reason.

Disposition values: `patched` (names the tag that shipped the fix) · `deferred` (carries a revisit trigger) · `no-change` (carries a reason).

| ID | Surfaced | Source project | Finding | Disposition | Tag / commit | Status |
|---|---|---|---|---|---|---|
| FB-01 | 2026-06-08 | stash | `env.schema.example` used zod-3 string APIs (`z.string().url()`); Better Auth 1.6.x requires zod 4, whose string formats are top-level (`z.url()`). A current-stack project must adapt the schema. | patched | v2026.06.9 | closed |
| FB-02 | 2026-06-08 | stash | `vitest.config.example.ts` counted `app/**`+`db/**` in the unit-coverage denominator, but the testing split routes Server Components / live DB client / auth seam to Playwright/integration — making the 70% unit gate unreachable-by-design. | patched | v2026.06.9 | closed |
| FB-03 | 2026-06-08 | stash | The preset shipped no `next.config.ts`; Better Auth pulls optional kysely adapter deps that webpack-bundles and fails `next build` until `serverExternalPackages: ['better-auth','pg']` is set. | patched | v2026.06.11 | closed |
| FB-04 | 2026-06-08 | stash | Better Auth needs four schema tables (`user`/`session`/`account`/`verification`) via `@better-auth/cli generate`; its default camelCase columns collide with data-modeling rule 1 (snake_case). The setup section didn't cover the generate step or the reconciliation. | patched | v2026.06.11 | closed |
| FB-05 | 2026-06-08 | stash | Calling `serverEnv()` (or constructing a DB/auth client) at module top-level throws at `next build` (env absent during route-module evaluation); `env.schema.ts` is import-safe but the caller discipline wasn't documented. | patched | v2026.06.11 | closed |
| FB-06 | 2026-06-08 | stash | The preset shipped no `package.json`, `tsconfig.json`, `.env.example`, or DB client — deps/scripts lived as prose in CLAUDE.partial, so a fresh project hand-assembled them (prose where a config should be). | patched | v2026.06.17 | closed |
| FB-07 | 2026-06-09 | studio (acme-devtools docs-site e2e) | A shipped dark theme was audited in one theme only; `github-dark` code-comment tokens were ~3:1. Dark mode requires AA-contrast verification in **both** themes (incl. syntax-highlight comment tokens). | deferred | trigger: promote to a library frontend-a11y standard + enforcement when a consuming project next ships dark mode (already encoded in sibling studio: cohesion-checklist §21/§22 + `theme-contrast-audit` check) | open |
| FB-08 | 2026-06-09 | studio (acme-devtools docs-site e2e) | Production build lacked branded error boundaries and its CSP `report-uri` pointed at a non-existent endpoint (404 on every violation). Shippable apps must mandate `error.tsx`/`global-error.tsx`/`loading.tsx` + a real, env-configurable CSP report endpoint — no dangling report-uri. | deferred | trigger: fold into a library production-readiness standard at next currency pass (encoded in studio frontend-developer §5.13) | open |
| FB-09 | 2026-06-09 | studio (acme-devtools docs-site e2e) | The ⌘K dialog set `aria-label` but not a Radix `DialogTitle` → console error + no screen-reader dialog name. Interactive dialogs need an accessible name via a real title element + description, not just `aria-label`. | deferred | trigger: library a11y-component standard (encoded in studio `dialog-accessible-name` check + cmdk reference) | open |
| FB-10 | 2026-06-09 | studio (acme-devtools docs-site e2e) | Rendered `<title>`s exceeded 60 chars because only the base string was measured, not base + layout `title.template`. Title-length checks must measure the **rendered** title (template-inclusive). | deferred | trigger: library SEO standard (encoded in studio lint `title-length` check + seo-specialist rule) | open |
| FB-11 | 2026-06-09 | studio (acme-devtools docs-site e2e) | An og:image audit passed on file-presence while the declared absolute prod-origin URL 404'd in local audits; and disclosure/FAQ pages emitted `TechArticle` JSON-LD instead of `FAQPage`. Structured-data/OG audits must fetch-resolve (200, mapping prod origin → audited origin) and assert `@type` matches page content. | deferred | trigger: library SEO/verification standard (encoded in studio `og-image-resolution` + `jsonld-type-expectation` checks) | open |
| FB-12 | 2026-06-09 | studio (criss + acme sites) | `output:'standalone'` nested `server.js` off-path via a stray parent lockfile, and shippable sites lacked deploy scaffolding (Dockerfile / standalone / vercel.json / `.env.example` / telemetry instrumentation hook). Shippable archetypes need deploy scaffolding + `outputFileTracingRoot`. | deferred | trigger: library deployment standard + preset at next currency pass (encoded in studio frontend-developer §5.13) | open |

## Notes

- **Patched** rows must name a tag that exists in the repo's `git tag` list — `check-flowback.sh` enforces this, so a "fixed" claim can't outrun the actual release. (Release ordering, like a changelog: commit the fix + this row, cut the tag, then push commit and tag together — `git push --follow-tags` — so CI never sees a patched row whose tag is absent.)
- **Deferred** rows must carry a non-empty revisit trigger (same data-driven discipline as calibration triggers); the currency pass walks them.
- The library cannot enumerate where consuming projects live, so harvesting findings *into* this log is a manual currency-pass step (`00-governance/standards-lifecycle.md` §6): for each known project, diff its debt-log "Library flow-back" section against the rows here and add any missing finding.
- A finding that also moved a calibration knob gets a paired row in `00-governance/calibration.md`'s Observations section (e.g. FB-02 ↔ the coverage-include observation).
